#include "perfkit/extension/net-provider.hpp"

#include "net_terminal.hpp"

perfkit::terminal_ptr perfkit::terminal::net_provider::create(
        std::string session_name,
        const struct init_info& info) {
  throw;
}
